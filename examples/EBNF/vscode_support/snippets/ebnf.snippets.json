{
  "Comment definition": {
    "prefix": "@ comment",
    "body": "@ comment = /$0/",
    "description": "Regular expression defining comments,\\nexample: /#.*(?:\\n|$)/"
  },
  "Whitespace definition": {
    "prefix": "@ whitespace",
    "body": "@ whitespace = /$0/",
    "description": "Regular expression defining insignificant whitespace,\\nexample: /\\s*/.\nAlternatively, one of the values horizontal, linefeed, vertical can be given.\n 'linefeed' means that a single linefeed, but not an empty sill counts as whitespace."
  },
  "Surrounding whitespace for literals": {
    "prefix": "@ literalws",
    "body": "@ literalws = $0",
    "description": "Selects the side on which implicitly capture whitespace,\\npossible values: right, left, both, none\\default value: right. Only a single value is possible!"
  },
  "Early dropping of irrelevant nodes": {
    "prefix": "@ drop",
    "body": "@ drop = $0, $1",
    "description": "Selects which kind of nodes will be dropped\\nfrom the conctrete syntax tree, already, \\npossible values: token, whitespace. Both values can be specified.\\nEmpty anonymous nodes and anonymous nodes with a single child will always be eliminated."
  },
  "Preprocessor token declaration": {
    "prefix": "@ tokens",
    "body": "@ tokens = $0, $1, $2, ...",
    "description": "Declare the names of the tokens that an (optional) preprocessor emits."
  },
  "Error message substituion": {
    "prefix": "@ _error",
    "body": "@ SYMBOL_error = /${0:regex}/, \"${1:message}\"",
    "description": "Error Message that shall be emitted if a mandatory (§) element is missed\nwhile parsing SYMBOL, but the regular expression matches the following text."
  },
  "Skip to next legitimate value after parsing failed": {
    "prefix": "@ _skip",
    "body": "@ SYMBOL_skip = /${0:regex}/",
    "description": "Specifies a reentry-point where parser SYMBOL continues\nif a mandatory (§) element was missing."
  },
  "Resume after parsing failed": {
    "prefix": "@ _resume",
    "body": "@ SYMBOL_resume = /${0:regex}/",
    "description": "Specifies a reentry-point where the parser calling parser SYMBOL continues\nif parser SYMBOL misses a mandatory (§) element."
  }
}

